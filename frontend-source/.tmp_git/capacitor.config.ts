import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.openwebui.client',
  appName: 'AdoetzGPT Enhanced',
  webDir: '../build'
};

export default config;
